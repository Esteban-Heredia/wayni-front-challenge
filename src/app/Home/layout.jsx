export const metadata = {
  title: "Home | Prueba tecnica",
  description: "Bienvenido al dashboard principal",
};

export default function HomeLayout({ children }) {
  return <section>{children}</section>;
}
